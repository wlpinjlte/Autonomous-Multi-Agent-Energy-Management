[project]
name = "decision-agent"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "torch",
    "gymnasium",
    "stable-baselines3",
    "tensorboard",
    "sinergym==3.12.0; sys_platform == 'linux'"
]
